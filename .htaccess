Options -Indexes

# Runtime secrets are PHP-only and should never be requested directly.
<FilesMatch "^(config|openai)\.php$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Deny from all
  </IfModule>
</FilesMatch>
